export enum ETopicReaction {
  LIKE = "LIKE",
  DISLIKE = "DISLIKE",
}

export enum ETopicVisibility {
  PUBLIC = "PUBLIC",
  UNIVERSITY_ONLY = "UNIVERSITY_ONLY",
}

export enum ETopicStatus {
  ACTIVE = "ACTIVE",
  DELETED = "DELETED",
  CLOSED = "CLOSED",
}
